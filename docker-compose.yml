version: '3.8'

services:
  node-app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    network_mode: "host"
